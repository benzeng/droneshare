Droneshare.Router.map(function () {
  //this.resource('mission', { path: '/mission/:mission_id' });
  //this.reosurce('user', { path: '/user/:user_id' });
  //this.resource('vehicle', { path: '/vehicle/:vehicle_id' });
});
